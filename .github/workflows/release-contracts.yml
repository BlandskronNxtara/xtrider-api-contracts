name: release-contracts

on:
  workflow_dispatch:

jobs:
  release-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Release automation is intentionally not implemented yet."
