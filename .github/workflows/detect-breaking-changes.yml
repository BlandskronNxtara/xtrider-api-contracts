name: detect-breaking-changes

on:
  pull_request:

jobs:
  manual-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sh scripts/detect-breaking-changes.sh
