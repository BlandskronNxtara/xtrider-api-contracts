---
name: schema-validation
description: Validar sintaxis y estructura basica de JSON Schemas y ejemplos.
---

# Schema Validation

Ejecuta:

```powershell
python scripts/validate-json-schemas.py
```

Revisa que cada schema tenga `$schema`, `$id` y `title`.
